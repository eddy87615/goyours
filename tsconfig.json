{
  "compilerOptions": {
    "baseUrl": ".", // 設定專案的根路徑
    "paths": {
      // 添加匯入別名
      "@/*": ["src/*"]
    }
  },
  "include": ["src"], // 指定要編譯的檔案範圍（通常是 src 資料夾）
  "exclude": ["node_modules"] // 排除 node_modules 資料夾
}
